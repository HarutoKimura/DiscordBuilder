// M2 entry point: discord.js v14 bot + orchestrator (slash commands, threads,
// build queue, Codex event summaries). Implemented after M1's CLI pipeline works.

console.log('DiscordBuilder bot: not implemented yet (M2)');
